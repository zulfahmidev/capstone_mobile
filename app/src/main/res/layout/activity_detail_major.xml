<?xml version="1.0" encoding="utf-8"?>
<ScrollView xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@drawable/bg"
    tools:context=".ui.home.major.DetailMajorActivity">

    <androidx.constraintlayout.widget.ConstraintLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content">

        <ImageView
            android:id="@+id/iv_detail_major"
            android:layout_width="330dp"
            android:layout_height="175dp"
            android:layout_marginTop="10dp"
            android:src="@drawable/img_detail_major"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintTop_toTopOf="parent"
            tools:ignore="ContentDescription" />

        <TextView
            android:id="@+id/tv_major_name"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:gravity="center"
            android:layout_marginTop="16dp"
            android:layout_marginHorizontal="20dp"
            android:textSize="19sp"
            android:textStyle="bold"
            android:text="@string/major_name"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintTop_toBottomOf="@+id/iv_detail_major" />

        <TextView
            android:id="@+id/tv_major_description"
            android:layout_width="0dp"
            android:layout_height="match_parent"
            android:text="@string/major_description"
            android:layout_marginVertical="16dp"
            android:justificationMode="inter_word"
            android:textSize="17sp"
            app:layout_constraintBottom_toBottomOf="parent"
            app:layout_constraintEnd_toEndOf="@+id/tv_major_name"
            app:layout_constraintStart_toStartOf="@+id/tv_major_name"
            app:layout_constraintTop_toBottomOf="@+id/tv_major_name"
            tools:targetApi="o" />

    </androidx.constraintlayout.widget.ConstraintLayout>

</ScrollView>